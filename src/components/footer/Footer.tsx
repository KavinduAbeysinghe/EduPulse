import { Box, Divider, Typography } from "@mui/material";
import React from "react";

export const Footer = () => {
  return (
    <Box mt={5}>
      <Divider />
      <Typography>sdv</Typography>
    </Box>
  );
};
